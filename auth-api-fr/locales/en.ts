import { TranslateLocale } from 'hooks/useLocale';

const en: TranslateLocale = {
  userId: 'USER ID',
  password: 'PASSWORD',
  login: 'LOGIN',
  authError: 'USER ID or PASSWORD is different',
};

export default en;
